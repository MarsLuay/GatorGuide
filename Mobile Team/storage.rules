rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() {
      return request.auth != null;
    }

    function ownsId(userId) {
      return signedIn() && request.auth.uid == userId;
    }

    match /users/{userId}/{allPaths=**} {
      allow read, write: if ownsId(userId);
    }

    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
