{
  "name": "gatorguide-functions",
  "private": true,
  "license": "SEE LICENSE IN LICENSE",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "lint": "node --check index.js && node --check supportEmail.js && node --check supportEmail.test.js && node --check geminiGateway.js && node --check opportunityGateway.js && node --check promptTemplates.js",
    "test": "node --test",
    "serve": "firebase emulators:start --only functions"
  },
  "dependencies": {
    "firebase-admin": "^13.7.0",
    "firebase-functions": "^7.2.5"
  },
  "overrides": {
    "fast-xml-builder": "1.1.7",
    "fast-xml-parser": "5.7.2",
    "protobufjs": "7.5.6",
    "node-forge": "1.4.0",
    "path-to-regexp": "0.1.13",
    "teeny-request": {
      "http-proxy-agent": "7.0.2"
    },
    "uuid": "14.0.0"
  }
}
