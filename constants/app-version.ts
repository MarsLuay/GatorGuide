/**
 * App version - single source of truth.
 * Update this value when releasing a new version.
 * Keep package.json and app.json (expo.version) in sync for build metadata.
 */
export const APP_VERSION = "1.0.0";
