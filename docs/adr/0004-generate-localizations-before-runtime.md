# Generate localizations before runtime

New and changed English interface copy will be translated during development into the existing locale bundles, with placeholder protection, an academic glossary, explicit overrides, and risk-based review flags. The app will continue shipping static translations so users never depend on runtime AI, network translation, added latency, or transcript-adjacent data disclosure.

